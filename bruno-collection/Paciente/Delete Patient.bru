meta {
  name: Delete Patient
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}{{apiPath}}/pacientes/1
  body: none
  auth: none
}

tests {
  test("Status should be 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
