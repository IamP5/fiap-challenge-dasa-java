meta {
  name: Get Doctor by ID
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}{{apiPath}}/medicos/1
  body: none
  auth: none
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Should return doctor object", function() {
    expect(res.getBody()).to.have.property('medicoId');
    expect(res.getBody()).to.have.property('nomeCompleto');
  });
}
